import fs from 'fs'; import path from 'path'; const root=process.cwd(); const dist=path.join(root,'dist'); if(fs.existsSync(dist)) fs.rmSync(dist,{recursive:true,force:true}); fs.mkdirSync(dist,{recursive:true}); const skip=new Set(['dist','node_modules','.git','scripts']); for(const e of fs.readdirSync(root,{withFileTypes:true})){ if(skip.has(e.name)) continue; fs.cpSync(path.join(root,e.name),path.join(dist,e.name),{recursive:true}); } console.log('Berthoud WiFi site copied to dist');